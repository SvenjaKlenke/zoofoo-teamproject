export type Animal = {
    id: string,
    species: string,
    food: string,
    foodAmount: number,
    dayToFeed: string,
    numberOfAnimals: number,
    feedStatus: string,
    animalKeeper: string,
    pictureOfAnimal: string;
}